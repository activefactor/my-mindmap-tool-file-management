# マインドマップツール 開発ステップ書
## Phase 2 — レンタルサーバー（heteml）移行版

**バージョン**: 1.4
**作成日**: 2026-07-25
**更新日**: 2026-07-31
**対象フェーズ**: Phase 2
**参照**: `docs/要件定義書.md` v2.1、`docs/基本設計書_Phase2.md` v2.1、`docs/開発標準ルール.md`、`docs/セキュリティポリシー.md`

> 本書は基本設計書_Phase2.md を実装可能な単位に分解したものです。各ステップは前のステップの
> 完了を前提とします（依存関係は各ステップの冒頭に明記）。ステップの区切りは PR の区切りとも
> 一致させることを想定しています（開発標準ルール.md §4.3「1 PR = 1 機能」）。

---

## 0. 全ステップ共通のルール

- ブランチは `feature/phase2-<ステップ名>` の形式で作成する（例: `feature/phase2-auth`）。
- 各ステップ完了時に開発ログを記録する。置き場所は `docs/開発標準ルール.md` §2.1 の基準に従う
  （コードを変更した場合は `mindmap-tool/docs/devlog/` または `server/docs/devlog/`、
  Step 0 のような確認・調査のみの場合はルート直下の `docs/devlog/`）。技術的な判断があった
  場合はコードを変更したディレクトリの `docs/adr/` にも記録する。
- 各ステップのチェックリストが全て埋まってから次のステップへ進む。埋まらない項目がある場合は
  未決定事項として本書または基本設計書_Phase2.md §12 に追記し、ユーザーに確認する。
- 本書のチェックリストは `docs/セキュリティポリシー.md` §7「Phase 2 追加チェックリスト」を
  補完するものであり、矛盾する場合はより具体的な本書の記載を優先し、セキュリティポリシー側も
  同じ内容に更新する。

---

## Step 0: 前提確認（未決定事項の解消）

**依存**: なし（最初に実施）
**対応**: 基本設計書_Phase2.md §12 の未決定事項 No.4, 5, 6

実装に着手する前に、heteml の実機仕様を確認し、基本設計書の暫定記述を確定させる。
2026-07-29 に heteml 公式サポート情報（公開ページ）を調査し、2026-07-31 にユーザーから
契約アカウント固有の値の提供を受けて確定した。

- [x] **確定（2026-07-31）**: 対象ドメイン `mindmap.activefactor.org` の公開ディレクトリは
      `/web/activefactor.org/mindmap/`。基本設計書_Phase2.md §1.3 に反映済み
- [x] **確定（2026-07-31）**: PHP 8.5（CGI版）を採用。基本設計書_Phase2.md §1.2 に反映済み、
      `composer.json` の `"php"` 制約は `"~8.5.0"` とする
- [x] **確定（2026-07-31）**: MySQL 8.4 を採用。8.0以上のため `JSON` 型は問題なく使える
      （`LONGTEXT` フォールバック不要）。基本設計書_Phase2.md §1.2 に反映済み
- [ ] 必要な PHP 拡張モジュール（`curl`, `openssl`, `zip`, `pdo_mysql`, `mbstring`）が
      PHP 8.5（CGI版）で有効であることを確認する（Step 1: Docker環境構築で、同バージョンの
      イメージを用いて確認する）
- [x] **確定（2026-07-31）**: cron は利用可能（[heteml cron設定ヘルプ](https://support.heteml.jp/hc/ja/articles/360042620493-cron-%E8%A8%AD%E5%AE%9A)）。
      レンタルサーバーの共有リソースのため高頻度なジョブは制限されるが、本設計で必要な
      ジョブ（Zipクリーンアップ1時間ごと、ゴミ箱自動完全削除1日1回程度）はいずれも低頻度で
      問題ない見込み。具体的なcron式・設定可能件数の上限は Step 11（本番デプロイ設定）で
      コントロールパネル上から登録する際に確認する
- [x] （一般仕様は確認済み）heteml は SSH 接続を全ユーザーに無料で提供しているが、
      **Composer はプリインストールされていない**。SSHでログイン後、
      `curl -sS https://getcomposer.org/installer | php` 等で自分のホームディレクトリ配下に
      手動インストールする必要がある（基本設計書_Phase2.md §9 に手順を追記済み）
- [x] （運用判断・実装ブロッカーではない。調査結果を記録）heteml の「海外アタックガード」は、
      WordPress/MovableType 等の**既知のCMS管理画面など、あらかじめ定義された特定の
      ディレクトリ・ファイル名のみ**を保護対象とする機能であることが公式情報から判明した。
      本アプリの独自ログイン／API経路（`/api/auth/*`等）がこの対象に含まれるかは公開情報
      からは確認できず、**対象外である可能性が高い**。有効化するかどうか、独自パスに
      適用できるか（heteml サポートへの問い合わせが必要な可能性あり）は構築・デプロイ時に
      判断する（基本設計書_Phase2.md §12 未決定事項 No.10 に記録済み）

**完了条件**: 上記すべてを確認し、基本設計書_Phase2.md の該当箇所（§1.2, §1.3, §5.1）を
実機情報で更新済みであること。

**ステータス**: 完了（2026-07-31）。PHP拡張モジュールの有効性確認のみ Step 1 に持ち越し。

---

## Step 1: ローカル開発環境（Docker）構築

**依存**: Step 0
**対応**: 基本設計書_Phase2.md §10

- [x] `docker-compose.yml` を作成し、PHP 8.5・MySQL 8.4（Step 0 で確定）に合わせた
      コンテナ（`php`, `mysql`）を定義した。heteml は CGI版PHPだが、ローカル開発では
      Apache+mod_php構成とした（実行方式の違いはデプロイ手順側で吸収する）
- [x] **検証済み（2026-07-31）**: PHP コンテナで `curl`, `openssl`, `zip`, `pdo_mysql`,
      `mbstring` 拡張が有効であることを `docker compose exec php php -m` で確認した
      （5つとも有効）
- [x] **検証済み（2026-07-31）**: `server/` を PHP コンテナにマウントし、
      `docker compose exec php composer install` が成功することを確認した
      （`composer.lock` を生成し、リポジトリにコミットする）
- [x] **検証済み（2026-07-31）**: フロントエンド用に Vite dev server は未起動だが、
      `curl http://localhost:8080/api/health` で `{"status":"ok","php_version":"8.5.9"}`
      （HTTP 200）を確認した。Vite からの疎通はブラウザ横断の CORS 設定が絡まないローカル
      環境のため、同様にアクセス可能と判断する
- [x] **検証済み（2026-07-31）**: `docker compose exec mysql mysql ...` で MySQL 8.4.11 に
      接続できることを確認した
- [ ] Google / Microsoft の開発者コンソールに、ローカル開発用のリダイレクトURI
      （`http://localhost:8080/api/auth/google/callback` 等）を登録する。
      **Google/Microsoftの開発者コンソールへのアクセスが必要なため、ユーザー側での対応が必要**
      （Step 3でOAuthを実装する前までに完了していればよく、Step 1完了の必須条件ではない）
- [x] `.env.example` を作成した（`server/.env.example`。基本設計書_Phase2.md §11 の内容
      をベースに、ローカルDocker用に `DB_HOST=mysql` をデフォルト値とした）
- [x] `server/README.md` にローカル起動手順を記載した

**完了条件**: `docker-compose up` でローカル環境が起動し、フロントエンドから API の
ヘルスチェックエンドポイント（疎通確認用に最小限のものを用意）へアクセスできること。

**ステータス**: **完了（2026-07-31）**。ユーザーの環境で Docker が利用可能になったため、
実際に `docker compose up -d --build` を実行し、ヘルスチェック・PHP拡張・Composer・
MySQL接続をすべて検証した。残るのは Google/Microsoft 開発者コンソールへのリダイレクトURI
登録のみで、これは Step 3（認証基盤）着手までに行えばよい。

---

## Step 2: DBスキーマ実装

**依存**: Step 1
**対応**: 基本設計書_Phase2.md §5.1

- [x] **完了（2026-07-31）**: `server/db/migrations/` に、基本設計書_Phase2.md §5.1 の
      全テーブル（`users`, `user_identities`, `allowed_domains`, `allowed_emails`,
      `folders`, `mindmaps`, `audit_logs`）を、依存関係順に番号を振った7つのDDLファイル
      （`0001_create_users.sql` 〜 `0007_create_audit_logs.sql`）として作成した。
      適用済みファイルは `schema_migrations` テーブルで管理する自前ランナ
      （`server/db/migrate.php`）を実装した
- [x] **検証済み（2026-07-31）**: `folders(id, user_id)` の複合UNIQUEキーと
      `mindmaps(folder_id, user_id)` の複合外部キーを、実際にDockerのMySQL上で検証した。
      別ユーザーのフォルダを指定した `INSERT` は `ERROR 1452 (23000)` で拒否されること、
      自分のフォルダおよび `folder_id = NULL`（ルート直下）は正常に `INSERT` できることを
      確認した
- [x] **検証済み（2026-07-31）**: `mindmaps.revision` は `DEFAULT 1`、`deleted_at` は
      `NULL`（通常）/ `NOT NULL`（ゴミ箱）で機能することを実データで確認した
- [x] **完了（2026-07-31）**: `server/db/seed.php` を作成した。`.env` の
      `INITIAL_ADMIN_EMAIL` / `INITIAL_ALLOWED_DOMAIN` から初期管理者・許可ドメイン・
      許可アドレスを投入できることを確認し、複数回実行しても重複しない
      （`INSERT IGNORE` / `ON DUPLICATE KEY UPDATE`）ことも確認した
- [x] **検証済み（2026-07-31）**: `docker compose exec php php db/migrate.php` を複数回実行し、
      2回目以降はすべて `skip` されることを確認した（冪等性OK）

**完了条件**: ローカルDocker環境でマイグレーション一式が実行でき、seedで初期管理者が
作成されることを確認済みであること。

**ステータス**: **完了（2026-07-31）**。

> **実装時に発見した不具合（重要）**: 当初 `migrate.php` はDDL実行を
> `beginTransaction()`/`commit()`/`rollBack()` で囲っていたが、MySQLのDDL（`CREATE TABLE`）は
> 実行時に暗黙のコミットが発生するため、その後の `commit()`/`rollBack()` がPDOの
> `PDOException: There is no active transaction` を引き起こしてスクリプトが異常終了する
> 不具合があった。DDLはトランザクションで囲わず、DDL成功後に `schema_migrations` への
> 記録を別操作として行う方式に修正した（詳細は `server/docs/devlog/` 参照）。
>
> また、`db/seed.php` の設計中に、基本設計書_Phase2.md §3.1 のアカウント解決ロジックに
> 欠陥（既存ユーザーに `user_identities` が0件の場合でも conflict 扱いになり、シードで
> 作成した初期管理者が永久にログインできなくなる）を発見し、設計書側を修正済み
> （v2.2→v2.3）。

---

## Step 3: 認証基盤（OAuth・IDトークン検証・セッション・許可判定）

**依存**: Step 2
**対応**: FR-07, NFR-S-5/6/9/10、基本設計書_Phase2.md §3.1, §6.1

このステップは Phase 2 の中で最もリスクが高い部分（外部レビューで重大指摘が集中した領域）
のため、他のステップより手厚くチェックリストを設ける。

### ライブラリ選定（ADR化必須）

- [ ] Google/Microsoft 双方の OAuth・OIDC 実装方式を検証し、ライブラリ（`league/oauth2-client`
      系か、IDトークン検証込みで自前実装するか）を確定する（基本設計書_Phase2.md §12 未決定
      事項 No.1）。選定理由を ADR に記録する

### 実装項目

- [ ] `state`（乱数・一回限り・有効期限10分・provider紐付け）、PKCE（`code_verifier`/
      `code_challenge` S256）、`nonce` の生成・セッション保存・検証を実装する
- [ ] Discovery Document・JWKS を用いたIDトークンの署名検証を実装する（`alg=none`拒否、
      `kid`によるキー選択）
- [ ] `iss`/`aud`/`exp`/`nonce` の検証を実装する。Microsoftは`iss`のテナント部分と`tid`
      クレームの整合を確認する
- [ ] UserInfoの`sub`とIDトークンの`sub`が一致することを確認する
- [ ] 許可判定ロジック（許可ドメイン／許可アドレス、大文字小文字を無視した比較）を実装する
- [ ] Googleログインで`hd`クレームによる追加チェックを実装する（該当する場合）
- [ ] identity解決ロジック（`provider`+`provider_user_id`で検索 → 無ければメール確認 →
      新規ユーザーの場合のみ作成、既存メールと衝突する場合は`login_denied_conflict`で拒否）
      を実装する
- [ ] セッションCookieを`HttpOnly`/`Secure`/`Path=/`/`SameSite=Lax`で発行する
- [ ] CSRFトークンの発行・`X-CSRF-Token`ヘッダー検証ミドルウェアを実装する
- [ ] `security_stamp`・`status`をリクエストごとに確認するミドルウェアを実装する
- [ ] ログイン成功・拒否（許可リスト外／衝突／無効化済み）をそれぞれ監査ログに記録する
      （トークン類は記録しない）
- [ ] ログアウトAPI（CSRF検証込み）を実装する
- [ ] アイドルタイムアウト（既定60分）を実装する

### テスト（基本設計書_Phase2_レビュー報告書_20260725.md §7「認証」を参照）

- [ ] `state`の不一致・期限切れ・再利用を拒否するテスト
- [ ] 不正署名・誤った`aud`・期限切れIDトークンを拒否するテスト
- [ ] Microsoftの`iss`と`tid`が不一致のトークンを拒否するテスト
- [ ] 同一メールで異なる`sub`（＝異なる主体）を自動統合しないテスト
- [ ] 無効化されたユーザーの既存セッションが次のリクエストで使えなくなるテスト

**完了条件**: 上記すべての実装・テストが完了し、Google/Microsoft 双方でログイン
（開発者コンソール登録済みのテストアカウント）が成功することを確認済みであること。

---

## Step 4: 管理コンソール API・画面

**依存**: Step 3
**対応**: FR-08

- [ ] ユーザー一覧API（ページネーション）を実装する
- [ ] ロール変更・無効化／再有効化APIを実装する。対象行を`SELECT ... FOR UPDATE`でロックし、
      同一トランザクション内で「更新後も管理者が1人以上残るか」を再確認してから更新する
- [ ] ロール変更・ステータス変更時に`security_stamp`を再生成し、監査ログに変更前後の値を
      記録する
- [ ] 許可ドメイン／許可アドレスのCRUD APIを実装する
- [ ] 監査ログ一覧API（ページネーション・日時範囲/操作種別フィルタ）を実装する
- [ ] ストレージ使用状況API（`SUM(LENGTH(data))`による概算）を実装する
- [ ] フロントエンド: `AdminConsolePage`, `UserTable`, `AllowedDomainList`,
      `AllowedEmailList`, `AuditLogTable` を実装する
- [ ] 一般ユーザーが`/admin`にアクセスした場合に403でダッシュボードへリダイレクトされることを
      確認する

### テスト

- [ ] 複数の管理者が同時に別の管理者を降格しても、管理者が0人にならないことを確認する
      並行テスト
- [ ] 自分自身をロール変更しようとした場合に拒否されることを確認する

**完了条件**: 管理者アカウントでユーザー管理・許可リスト管理・監査ログ閲覧が一通り
動作すること。

---

## Step 5: マインドマップ CRUD API（ソフトデリート含む）

**依存**: Step 3（認証が前提）
**対応**: FR-09, FR-12, FR-14、基本設計書_Phase2.md §6.2

- [ ] 一覧・取得・作成・更新（オートセーブ）APIを実装する。更新APIは`revision`必須とし、
      不一致時は409を返す
- [ ] サーバー側バリデーション（基本設計書_Phase2.md §6.2 の各上限値）を実装する
- [ ] 所有者チェック（`user_id`不一致は404）を全エンドポイントに適用する
- [ ] ソフトデリートAPI（`DELETE`で`deleted_at`設定）、ゴミ箱一覧API、復元API、完全削除
      （purge）APIを実装する
- [ ] 保持期間（`TRASH_RETENTION_DAYS`）経過後にゴミ箱内マップを自動的に完全削除する
      バッチ（cron）を実装する

### テスト

- [ ] 他ユーザーのマップを取得・更新・削除・ダウンロードできないことを確認するテスト
- [ ] 上限を超えるJSON・ノード数・深さ・テキスト長を拒否するテスト
- [ ] 削除→ゴミ箱一覧に表示→復元→通常一覧に戻ることを確認するテスト
- [ ] 保持期間経過後に自動完全削除されることを確認するテスト（時刻をモック、またはバッチを
      手動実行して検証）

**完了条件**: マップの作成・編集・削除・復元・完全削除が一通り動作し、他ユーザーのデータへ
アクセスできないことを確認済みであること。

---

## Step 6: フォルダ管理 API

**依存**: Step 5
**対応**: FR-10

- [ ] フォルダのCRUD APIを実装する
- [ ] マップ移動API（`PUT /api/mindmaps/{id}/move`）で、移動先フォルダの所有者が
      現在のユーザーであることをアプリケーション層で検証する（DB複合FKと合わせた二重チェック）
- [ ] フォルダ削除時、内包するマップをルート直下（`folder_id = NULL`）に移動してから
      フォルダを削除する

### テスト

- [ ] 他ユーザーのフォルダへ自分のマップを移動できないことを確認するテスト（アプリ層・
      DB制約の両方で検証されることを確認する）
- [ ] フォルダ削除後もマップが消えず、ルート直下に表示されることを確認するテスト

**完了条件**: フォルダの作成・名称変更・削除・マップ移動が一通り動作すること。

---

## Step 7: ダッシュボード画面

**依存**: Step 5, Step 6
**対応**: FR-09, FR-10, FR-14

- [ ] `DashboardPage` を実装し、マップ一覧・フォルダ一覧を表示する
- [ ] マップ項目をフォルダへドラッグ＆ドロップで移動できるようにする
- [ ] 右クリック／メニューから「開く」「ダウンロード」「フォルダへ移動」「削除（ゴミ箱へ）」
      を実行できるようにする
- [ ] フォルダの「Zipダウンロード」「名称変更」「削除」メニューを実装する
- [ ] `TrashPage`（ゴミ箱）を実装し、復元・完全削除を行えるようにする
- [ ] マップを選択すると、`binding: server(id, revision)` として編集画面が開くことを
      確認する

**完了条件**: ログインからダッシュボード表示、マップを開く、フォルダ操作、ゴミ箱操作までが
一通りブラウザ上で動作すること。

---

## Step 8: ダウンロード機能（個別・Zip・自動削除）

**依存**: Step 5, Step 6
**対応**: FR-11、基本設計書_Phase2.md §3.5, §6.2

- [ ] 個別ファイルダウンロードAPIを実装する（`title`をサニタイズしたファイル名）
- [ ] フォルダZipダウンロードAPIを実装する。事前にフォルダ内マップ件数・合計サイズが
      上限以内かを確認し、超過時はエラーを返す
- [ ] ユーザーごと・アプリ全体の同時Zip生成数を制限する
- [ ] 一時ファイルをユーザーごとのサブディレクトリに、推測困難なランダムファイル名で生成する
- [ ] レスポンス送信直後の`unlink()`に加えて、`finally`ブロックと
      `register_shutdown_function()`による削除保証を実装する
- [ ] cronによる残留ファイルクリーンアップ（30分以上前に作成されたファイルを1時間ごとに
      削除）を実装する

### テスト

- [ ] 上限を超えるフォルダのZip生成が拒否されることを確認するテスト
- [ ] ダウンロード中断（クライアント切断等）をシミュレートしても、一時ファイルが残らない
      ことを確認するテスト
- [ ] cronのクリーンアップ対象パターンが、無関係なファイルを削除しないことを確認するテスト

**完了条件**: 個別ダウンロード・フォルダZipダウンロードが動作し、Zipファイルがサーバー上に
残留しないことを確認済みであること。

---

## Step 9: フロントエンドの保存／オープン状態管理統合

**依存**: Step 5, Step 7
**対応**: FR-12, FR-13、基本設計書_Phase2.md §3.6, §3.7

- [ ] `binding`（`server(id, revision)` / `local`）と `autosaveTarget`（`server` / `local`）
      の状態を分離して実装する
- [ ] `useServerAutosave` フックで、保存リクエストの直列化（1件のみin-flight）・
      デバウンス・`revision`送信を実装する
- [ ] サーバーからの409応答時に、ユーザーへ再読み込みを促す通知を表示する
- [ ] 「一覧に戻る」操作時に、保留中の変更をflushし、保存完了（または失敗）を待ってから
      遷移することを実装する
- [ ] 「開く」でローカルファイルを読み込んだ場合、必ず`binding = local`（新規セッション）に
      切り替わり、既存の`mindmapId`が破棄されることを確認する
- [ ] `autosaveTarget`のみを`local`に切り替えた場合は`mindmapId`が維持されることを確認する
- [ ] サーバー保存失敗時、IndexedDBに`${user_id}_${mindmap_id}`をキーとしてフォールバック
      保存することを実装する

### テスト

- [ ] 最後の編集直後に「一覧に戻る」を押しても内容が保存されることを確認するテスト
- [ ] ローカルファイルを開いてもサーバー上のマップが上書きされないことを確認するテスト
- [ ] 別ユーザーでログインした際に前ユーザーのローカルフォールバックが表示されないことを
      確認するテスト

**完了条件**: オートセーブ・保存先切替・開く動作がレビュー指摘（データ消失・誤上書き）を
再現しないことを確認済みであること。

---

## Step 10: セキュリティ強化・運用まわり

**依存**: Step 3〜9（各APIが揃った後に横断的に適用）
**対応**: NFR-S-*、基本設計書_Phase2.md §6

- [ ] 認証・保存・ダウンロード・管理APIにレート制限を実装する
- [ ] `.htaccess`にHTTPSリダイレクト・`Strict-Transport-Security`・本番用CSP・
      `X-Content-Type-Options`・`X-Frame-Options`・`Referrer-Policy`を設定する
- [ ] API系レスポンスに`Cache-Control: no-store`を設定する
- [ ] 本番環境で`display_errors=Off`とし、クライアントには一般化したエラー＋相関IDのみ返す
- [ ] ログ・監査ログにトークン／マップ本文が出力されないことをコードレビューで確認する

**完了条件**: `docs/セキュリティポリシー.md` §7「Phase 2 追加チェックリスト」の全項目に
チェックが入ること。

---

## Step 11: 本番デプロイ設定

**依存**: Step 0〜10
**対応**: 基本設計書_Phase2.md §1.3, §11

- [ ] Step 0で確定した実際のディレクトリ構成に沿ってデプロイスクリプトを作成する
      （公開領域に`.env`/`vendor/`/`storage/`が含まれないことを検証する工程を含む）
- [ ] 本番用`.env`をheteml上に設定する（Gitには含めないことを再確認する）
- [ ] heteml cronにZipクリーンアップ・ゴミ箱自動完全削除のジョブを設定する
- [ ] バックアップ対象・RPO/RTOを確認し、`docs/基本設計書_Phase2.md` §6.6を確定情報で更新する
- [ ] 「海外アタックガード設定」を有効化するかどうかを判断し、判断内容をdevlogに記録する

**完了条件**: 本番環境（heteml）でログイン〜マップ保存〜ダウンロードの一連の操作が
動作することを確認済みであること。

---

## Step 12: 総合テスト・リリース前チェック

**依存**: Step 0〜11

- [ ] `docs/基本設計書_Phase2_レビュー報告書_20260725.md` §7「最低限必要な追加テスト」を
      全項目実施する
- [ ] `docs/セキュリティポリシー.md` §7 Phase 1/Phase 2 チェックリストを全項目確認する
- [ ] `docs/開発標準ルール.md` §5.5 のドキュメント更新チェックリストを確認する
      （要件定義書・基本設計書・devlogが実装と乖離していないか）
- [ ] `npm audit` / Composerパッケージの既知脆弱性チェックを実施する

**完了条件**: 上記すべてのチェックリストが完了し、リリース判断ができる状態であること。

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.0 | 2026-07-25 | 初版作成（基本設計書_Phase2.md v2.0 を基に、Step 0〜12 の開発ステップとチェックリストを作成） |
| 1.1 | 2026-07-29 | Step 0 に着手。heteml公式サポート情報の調査結果を反映し、一般仕様として確認できた項目にチェックを入れ、契約アカウント固有の確認が必要な項目を明確化した。§0共通ルールのdevlog置き場所の記載を開発標準ルール.md §2.1と整合させた |
| 1.2 | 2026-07-31 | Step 0 をユーザー提供の実機情報（ドメイン・PHP/MySQLバージョン等）で完了。Step 1（Docker環境構築）に着手し、`server/`一式（composer.json, .env.example, Dockerfile, docker-compose.yml, ヘルスチェック用フロントコントローラ, README）を作成。実行環境にDockerが無く`docker compose up`での動作確認は開発者側での実施が必要である旨を明記 |
| 1.3 | 2026-07-31 | ユーザー環境でDockerが利用可能になったため、`docker compose up -d --build`を実際に実行しStep 1を完了。ヘルスチェック（200 OK）・PHP拡張5種（curl/openssl/zip/pdo_mysql/mbstring）・`composer install`・MySQL 8.4接続をすべて検証済み。`composer.lock`をリポジトリに追加 |
| 1.4 | 2026-07-31 | Step 2（DBスキーマ実装）完了。migrations一式・自前マイグレーションランナ・seedスクリプトを作成し、Docker上のMySQLで冪等性・複合FK制約（他人のフォルダへの不正な紐付けを拒否）を実際に検証。実装時にDDLのトランザクション扱いに関する不具合と、基本設計書のアカウント解決ロジックの欠陥（初期管理者が永久にログインできない）を発見し、いずれも修正済み |
